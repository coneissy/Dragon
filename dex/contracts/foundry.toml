[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"
via_ir = false

[rpc_endpoints]
base = "${BASE_RPC_HTTP}"

[fmt]
line_length = 120
